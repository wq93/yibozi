import styled from 'styled-components';

export default styled.div`
  .article-list {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    justify-content: space-between;
    margin-top: 22px;
  }
  .empty-wrapper {
    margin: 0 auto;
  }
`;